'use strict';
const fs=require('fs'),path=require('path'),{spawnSync}=require('child_process');const root=path.resolve(__dirname,'..');
const files=['main.js','preload.js','src/renderer.js','src/shared/normalize.js','src/main/database.js','src/main/logger.js','src/main/service.js',...fs.readdirSync(__dirname).filter(x=>x.endsWith('.js')).map(x=>`scripts/${x}`),...fs.readdirSync(path.join(root,'tests'),{withFileTypes:true}).filter(x=>x.isFile()&&x.name.endsWith('.js')).map(x=>`tests/${x.name}`)];
for(const file of [...new Set(files)]){const r=spawnSync(process.execPath,['--check',file],{cwd:root,encoding:'utf8'});if(r.status!==0){process.stderr.write(r.stderr);process.exit(r.status||1);}const text=fs.readFileSync(path.join(root,file),'utf8');if(/nodeIntegration\s*:\s*true|contextIsolation\s*:\s*false|innerHTML\s*=\s*`[^`]*\$\{e\.(message|stack)/.test(text))throw new Error(`Güvenlik denetimi başarısız: ${file}`);}console.log(`${files.length} JavaScript dosyası denetlendi.`);
